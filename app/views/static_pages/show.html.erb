<%= content_for(:angular_app) do %>
  ng-app="testApp"
<% end %>

<%= content_for(:angular_app_controller) do %>
  ng-controller="sideBarCtrl"
<% end %>
<div class="container">
  <div>
    <button ng-click="initiateComponents('toggleButton')">Build Shiny button</button>
    <button ng-click="initiateComponents('authorBox')">Build Author Box</button>
  </div>

  <row ng-repeat="row in rows()" row="row"></row>

</div>
